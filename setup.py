from setuptools import find_packages, setup

setup(
    name='pyfit',
    packages=find_packages(include=['pyfit']),
    version='0.1.0',
    description='My first Python library',
    author='Me',
    license='MIT',
)
